# -*- coding: utf-8 -*-

# 네이버 블로그 발행에만 쓴다. 발행을 끄면 chromium 과 함께 뺄 수 있다.
selenium>=4.15.0
requests>=2.31.0

openai>=1.65.0
python-dotenv>=1.0.0

# 워커는 DB 에 직접 쓰지 않는다. 저장은 백엔드의 워커 전용 API 를 거친다.
APScheduler>=3.10.0
fastapi>=0.115.0
uvicorn[standard]>=0.34.0

